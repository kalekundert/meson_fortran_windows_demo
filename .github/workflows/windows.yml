name: Windows

on:
  push:
  workflow_dispatch:

jobs:
  test-windows:
    name: Test (Windows)
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4

    - run: |
        choco install meson

    - run: |
        meson setup build
        meson compile -C build

    - run: |
        build/hello

